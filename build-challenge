#!/bin/bash
# Build Challenge Launcher
# Runs the recording app with the correct Python environment

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
cd "$SCRIPT_DIR"

# Use the venv Python directly
.venv/bin/python recording_app.py "$@"
