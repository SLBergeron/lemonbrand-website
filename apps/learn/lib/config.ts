/**
 * Sprint course configuration
 * Toggle this to switch between waitlist mode and live enrollment
 */
export const SPRINT_CONFIG = {
  /**
   * When true, shows waitlist signup instead of checkout/enrollment CTAs
   * Set to false when the course is ready to launch
   */
  waitlistMode: true,
} as const;
